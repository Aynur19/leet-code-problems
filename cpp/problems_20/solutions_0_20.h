#pragma once

#include <tuple>
#include <vector>

class Solutions_0_20 {
public:
    std::vector<int> problem_1_twoSum(const std::vector<int>& nums, int target);

    std::vector<int> problem_2_twoSum(const std::vector<int>& nums, int target);
};